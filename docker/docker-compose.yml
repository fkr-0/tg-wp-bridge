services:
  tg-wp-bridge:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: tg-wp-bridge
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      TELEGRAM_BOT_TOKEN: "${TELEGRAM_BOT_TOKEN}"
      TELEGRAM_WEBHOOK_SECRET: "${TELEGRAM_WEBHOOK_SECRET}"
      PUBLIC_BASE_URL: "${PUBLIC_BASE_URL}"
      WP_BASE_URL: "${WP_BASE_URL}"
      WP_USERNAME: "${WP_USERNAME}"
      WP_APP_PASSWORD: "${WP_APP_PASSWORD}"
      WP_CATEGORY_ID: "${WP_CATEGORY_ID:-0}"
      WP_PUBLISH_STATUS: "${WP_PUBLISH_STATUS:-publish}"
      WP_POST_TYPE: "${WP_POST_TYPE:-post}"
      WP_USE_FEATURED_MEDIA: "${WP_USE_FEATURED_MEDIA:-false}"
      TG_WP_MAPPING_FILE: "${TG_WP_MAPPING_FILE:-/app/data/message_map.json}"
    ports:
      - "8000:8000"
    volumes:
      - ../data:/app/data:rw
